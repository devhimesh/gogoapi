services:
  - type: web
    name: webdis
    repo: https://github.com/devhimesh/gogoapi.git
    plan: free
    branch: main
    runtime: node  # specify the correct runtime here
    startCommand: node lib/api.js  # specify the start command here
    buildCommand: yarn
